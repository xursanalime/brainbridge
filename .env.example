BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: path to the JSON backup file. On Railway, point this at a
# mounted Volume (e.g. /data/words_backup.json) so it survives redeploys.
# BACKUP_JSON_PATH=/data/words_backup.json
